# SPDX-License-Identifier: LicenseRef-FNCL-1.1
# Copyright (c) 2026 Cpt_Kirk
.vscode/
build/
build_*/
build-panel*/
lvgl_design/
sdkconfig
sdkconfig.panel*
sdkconfig.old
# Local (personal) overlays contain Wi-Fi / HA credentials - never commit.
# The clean, credential-free build uses sdkconfig.defaults.s3 +
# sdkconfig.defaults.panels3 (see README).
sdkconfig.defaults.lazienka
sdkconfig.defaults.kuchnia
sdkconfig.lazienka
sdkconfig.kuchnia

# Internal merge / porting notes - never publish.
portieren.md
portieren-s3.md
MERGE-PROTOCOL.md

# Build-time downloads (regenerated by `idf.py` on configure).
managed_components/

# Panel variant manifest is generated from main/idf_component.panel{4,10,s3}.yml
# by the top-level CMakeLists.txt on every configure. Do not edit or commit.
main/idf_component.yml
